import styled from 'styled-components';

const PostWrap = styled.div`
  width: 100%;
`;

const Title = styled.h2`
  font-size: 18px;
  font-weight: 600;
  margin-bottom: 24px;
`;

export const style = {
  PostWrap,
  Title,
};
